{
  "name": "gestion-stock",
  "version": "1.0.0",
  "description": "Application de gestion de deux stocks avec rôles (admin, gestionnaire, lecteur)",
  "main": "server/index.js",
  "scripts": {
    "setup": "npm install && npm --prefix client install && npm --prefix client run build",
    "build": "npm --prefix client run build",
    "start": "node server/index.js",
    "dev": "node server/index.js"
  },
  "dependencies": {
    "adm-zip": "^0.5.16",
    "bcryptjs": "^2.4.3",
    "cookie-parser": "^1.4.7",
    "better-sqlite3": "^11.10.0",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2",
    "multer": "^1.4.5-lts.2"
  },
  "engines": {
    "node": ">=18"
  }
}
